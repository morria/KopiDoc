<% if(type.isPrimitive) {%>
  <span class="type primitiveType">
<% } else { %>
  <a rel="type" href="#" 
        title="<%= title ? type.qualifiedName + "\n" + title : type.qualifiedName %>"
        data-qualifiedName="<%= type.qualifiedName  %>">
<% } %>
<%= type.simpleTypeName %>
<% if(type.isPrimitive) {%>
  </span>
<% } else { %>
  </a>
<% } %>
