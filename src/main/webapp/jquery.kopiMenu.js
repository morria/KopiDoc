/**
  * 
  */
(function($) {
  var opts;
    $.fn.kopiMenu = function(options, fileList)
    {
        return this.each(function()
             {
                 
             });
    };
})(jQuery);

$.fn.kopiMenu.defaults = 
    {
        
    };